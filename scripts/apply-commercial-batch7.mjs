import { readFileSync, writeFileSync } from 'node:fs';

function patch(path,mutate){const before=readFileSync(path,'utf8');const after=mutate(before);if(after===before)throw new Error(`No changes applied to ${path}`);writeFileSync(path,after);}
function replaceOnce(source,from,to,label){const first=source.indexOf(from);if(first<0)throw new Error(`Missing ${label}`);if(source.indexOf(from,first+from.length)>=0)throw new Error(`Ambiguous ${label}`);return source.slice(0,first)+to+source.slice(first+from.length);}
function replaceAllChecked(source,from,to,label,min=1){const count=source.split(from).length-1;if(count<min)throw new Error(`Missing ${label}`);return source.split(from).join(to);}

patch('src/types.ts',source=>{
  source=replaceOnce(source,"export type PaymentMethod = 'cash' | 'bank-transfer' | 'card' | 'cheque' | 'other';\nexport type DiscountMode = 'fixed' | 'percent';","export type PaymentMethod = 'cash' | 'bank-transfer' | 'card' | 'cheque' | 'other';\nexport type PricingMethod = 'markup' | 'margin';\nexport type DiscountMode = 'fixed' | 'percent';",'pricing method type');
  source=replaceOnce(source,"export interface BankDetails {\n  bankName: string;\n  accountName: string;\n  iban: string;\n  swift: string;\n  currency: string;\n}\n","export interface BankDetails {\n  bankName: string;\n  accountName: string;\n  iban: string;\n  swift: string;\n  currency: string;\n}\n\nexport interface BankAccount extends BankDetails {\n  id: string;\n  label: string;\n}\n\nexport interface TaxPreset {\n  id: string;\n  name: string;\n  rate: string;\n}\n\nexport interface PaymentTermPreset {\n  id: string;\n  label: string;\n  days: number;\n}\n\nexport interface PricingPolicy {\n  method: PricingMethod;\n  percent: string;\n  rounding: string;\n}\n\nexport interface CommercialControls {\n  taxPresets: TaxPreset[];\n  defaultTaxPresetId: string;\n  paymentTermPresets: PaymentTermPreset[];\n  defaultPaymentTermPresetId: string;\n  pricing: PricingPolicy;\n}\n",'commercial control interfaces');
  source=replaceOnce(source,"  bank: BankDetails;\n  signatureDataUrl: string;","  bank: BankDetails;\n  bankAccounts: BankAccount[];\n  defaultBankAccountId: string;\n  commercial: CommercialControls;\n  signatureDataUrl: string;",'company commercial fields');
  source=replaceOnce(source,"  commercialRegistration: string;\n  notes: string;\n}","  commercialRegistration: string;\n  preferredCurrency: string;\n  paymentTermPresetId: string;\n  paymentTerms: string;\n  paymentDueDays: string;\n  creditLimit: string;\n  creditCurrency: string;\n  notes: string;\n}",'customer commercial fields');
  source=replaceOnce(source,"  voidedAt: string;\n  voidReason: string;\n  number: string;","  voidedAt: string;\n  voidReason: string;\n  bankAccountId: string;\n  paymentTermPresetId: string;\n  number: string;",'document commercial selectors');
  return source;
});

patch('src/lib/defaults.ts',source=>{
  source=replaceOnce(source,"import type { AppSettings, CompanySettings, CustomerSnapshot, CompanySnapshot, VaultPayload } from '../types.js';","import type { AppSettings, CompanySettings, CustomerSnapshot, CompanySnapshot, VaultPayload } from '../types.js';\nimport { defaultBankDetails } from './commercial-controls.js';",'default bank helper import');
  source=replaceOnce(source,"// v9 adds encrypted internal cost metadata for profitability analysis.\nexport const APP_SCHEMA_VERSION = 9;","// v9 adds encrypted internal cost metadata for profitability analysis.\n// v10 adds commercial controls, bank choices and customer credit policy.\nexport const APP_SCHEMA_VERSION = 10;",'schema v10');
  source=replaceOnce(source,"    vatNumber: '', taxNumber: '', commercialRegistration: '',\n    bank: { bankName: '', accountName: '', iban: '', swift: '', currency: 'USD' },\n    signatureDataUrl: '', stampDataUrl: '', defaultCurrency: 'USD', defaultLanguage: 'en',","    vatNumber: '', taxNumber: '', commercialRegistration: '',\n    bank: { bankName: '', accountName: '', iban: '', swift: '', currency: 'USD' },\n    bankAccounts: [], defaultBankAccountId: 'primary',\n    commercial: {\n      taxPresets: [], defaultTaxPresetId: '',\n      paymentTermPresets: [\n        { id:'term-due', label:'Due on receipt', days:0 },\n        { id:'term-net7', label:'Net 7', days:7 },\n        { id:'term-net15', label:'Net 15', days:15 },\n        { id:'term-net30', label:'Net 30', days:30 },\n        { id:'term-net45', label:'Net 45', days:45 },\n        { id:'term-net60', label:'Net 60', days:60 }\n      ],\n      defaultPaymentTermPresetId: '', pricing:{ method:'markup', percent:'0', rounding:'0.01' }\n    },\n    signatureDataUrl: '', stampDataUrl: '', defaultCurrency: 'USD', defaultLanguage: 'en',",'default commercial controls');
  source=replaceOnce(source,"    bank: { ...company.bank }, signatureDataUrl: company.signatureDataUrl, stampDataUrl: company.stampDataUrl,","    bank: { ...defaultBankDetails(company) }, signatureDataUrl: company.signatureDataUrl, stampDataUrl: company.stampDataUrl,",'snapshot selected default bank');
  return source;
});

patch('src/storage/vault.ts',source=>{
  source=replaceOnce(source,"function nowIso(): string { return new Date().toISOString(); }","function nowIso(): string { return new Date().toISOString(); }\nfunction normalizeBankAccounts(value:unknown):any[]{\n  if(!Array.isArray(value))return[];const seen=new Set<string>();const result:any[]=[];\n  value.forEach((account:any,index)=>{const id=stringValue(account?.id,`bank-${index+1}`).trim()||`bank-${index+1}`;if(id==='primary'||seen.has(id))return;seen.add(id);result.push({id,label:stringValue(account?.label,`Bank ${index+2}`),bankName:stringValue(account?.bankName),accountName:stringValue(account?.accountName),iban:stringValue(account?.iban),swift:stringValue(account?.swift),currency:cleanCurrency(account?.currency,'USD')});});return result;\n}\nfunction normalizeTaxPresets(value:unknown):any[]{if(!Array.isArray(value))return[];const seen=new Set<string>();const result:any[]=[];value.forEach((preset:any,index)=>{const id=stringValue(preset?.id,`tax-${index+1}`).trim()||`tax-${index+1}`;if(seen.has(id))return;seen.add(id);result.push({id,name:stringValue(preset?.name),rate:stringValue(preset?.rate,'0')});});return result;}\nfunction normalizePaymentTermPresets(value:unknown,fallback:any[]):any[]{if(!Array.isArray(value))return fallback.map(item=>({...item}));const seen=new Set<string>();const result:any[]=[];value.forEach((preset:any,index)=>{const id=stringValue(preset?.id,`term-${index+1}`).trim()||`term-${index+1}`;if(seen.has(id))return;seen.add(id);result.push({id,label:stringValue(preset?.label),days:Math.min(3650,Math.max(0,Math.trunc(finiteNumber(preset?.days,0))))});});return result;}\nfunction normalizeCommercial(value:any,fallback:any):any{const source=value&&typeof value==='object'?value:{};return{taxPresets:normalizeTaxPresets(source.taxPresets),defaultTaxPresetId:stringValue(source.defaultTaxPresetId),paymentTermPresets:normalizePaymentTermPresets(source.paymentTermPresets,fallback.paymentTermPresets),defaultPaymentTermPresetId:stringValue(source.defaultPaymentTermPresetId),pricing:{method:source.pricing?.method==='margin'?'margin':'markup',percent:stringValue(source.pricing?.percent,fallback.pricing.percent),rounding:['0.01','0.05','0.10','0.50','1.00'].includes(source.pricing?.rounding)?source.pricing.rounding:fallback.pricing.rounding}};}", 'migration commercial normalizers');
  source=replaceOnce(source,"    bank:{\n      bankName:stringValue(sourceBank.bankName,defaultBank.bankName), accountName:stringValue(sourceBank.accountName,defaultBank.accountName), iban:stringValue(sourceBank.iban,defaultBank.iban),\n      swift:stringValue(sourceBank.swift,defaultBank.swift), currency:cleanCurrency(sourceBank.currency,defaultBank.currency)\n    },\n    signatureDataUrl:stringValue(sourceCompany.signatureDataUrl),","    bank:{\n      bankName:stringValue(sourceBank.bankName,defaultBank.bankName), accountName:stringValue(sourceBank.accountName,defaultBank.accountName), iban:stringValue(sourceBank.iban,defaultBank.iban),\n      swift:stringValue(sourceBank.swift,defaultBank.swift), currency:cleanCurrency(sourceBank.currency,defaultBank.currency)\n    },\n    bankAccounts:normalizeBankAccounts(sourceCompany.bankAccounts), defaultBankAccountId:stringValue(sourceCompany.defaultBankAccountId,'primary')||'primary',\n    commercial:normalizeCommercial(sourceCompany.commercial,defaults.company.commercial),\n    signatureDataUrl:stringValue(sourceCompany.signatureDataUrl),",'migrate company commercial fields');
  source=replaceOnce(source,"  };\n\n  const sourceSettings = (vault as any).appSettings ?? {};","  };\n  if(!new Set(['primary',...migrated.company.bankAccounts.map(account=>account.id)]).has(migrated.company.defaultBankAccountId))migrated.company.defaultBankAccountId='primary';\n  if(migrated.company.commercial.defaultTaxPresetId&&!migrated.company.commercial.taxPresets.some(item=>item.id===migrated.company.commercial.defaultTaxPresetId))migrated.company.commercial.defaultTaxPresetId='';\n  if(migrated.company.commercial.defaultPaymentTermPresetId&&!migrated.company.commercial.paymentTermPresets.some(item=>item.id===migrated.company.commercial.defaultPaymentTermPresetId))migrated.company.commercial.defaultPaymentTermPresetId='';\n\n  const sourceSettings = (vault as any).appSettings ?? {};",'validate migrated commercial defaults');
  source=replaceOnce(source,"    vatTaxNumber:stringValue(customer?.vatTaxNumber), commercialRegistration:stringValue(customer?.commercialRegistration), notes:stringValue(customer?.notes)\n  })) : [];","    vatTaxNumber:stringValue(customer?.vatTaxNumber), commercialRegistration:stringValue(customer?.commercialRegistration),\n    preferredCurrency:cleanCurrency(customer?.preferredCurrency,'').replace(/^USD$/,'')?cleanCurrency(customer?.preferredCurrency,''):stringValue(customer?.preferredCurrency).trim().toUpperCase(), paymentTermPresetId:stringValue(customer?.paymentTermPresetId), paymentTerms:stringValue(customer?.paymentTerms), paymentDueDays:stringValue(customer?.paymentDueDays), creditLimit:stringValue(customer?.creditLimit), creditCurrency:stringValue(customer?.creditCurrency).trim().toUpperCase(), notes:stringValue(customer?.notes)\n  })) : [];",'migrate customer commercial fields');
  source=replaceOnce(source,"      id:stringValue(document?.id), kind:document?.kind === 'invoice' ? 'invoice' : 'proforma', role:document?.role==='credit-note'?'credit-note':'standard', status:document?.status === 'final' ? 'final' : 'draft', lifecycleStatus:document?.lifecycleStatus==='voided'?'voided':'active', revision:Math.max(1,Math.trunc(finiteNumber(document?.revision,1))), creditForId:stringValue(document?.creditForId), creditForNumber:stringValue(document?.creditForNumber), voidedAt:stringValue(document?.voidedAt), voidReason:stringValue(document?.voidReason),\n      number:stringValue(document?.number),","      id:stringValue(document?.id), kind:document?.kind === 'invoice' ? 'invoice' : 'proforma', role:document?.role==='credit-note'?'credit-note':'standard', status:document?.status === 'final' ? 'final' : 'draft', lifecycleStatus:document?.lifecycleStatus==='voided'?'voided':'active', revision:Math.max(1,Math.trunc(finiteNumber(document?.revision,1))), creditForId:stringValue(document?.creditForId), creditForNumber:stringValue(document?.creditForNumber), voidedAt:stringValue(document?.voidedAt), voidReason:stringValue(document?.voidReason), bankAccountId:stringValue(document?.bankAccountId), paymentTermPresetId:stringValue(document?.paymentTermPresetId),\n      number:stringValue(document?.number),",'migrate document commercial selectors');
  return source;
});

patch('src/storage/vault-merge.ts',source=>{
  const from=`function mergeCompany(base:CompanySettings,intended:CompanySettings,latest:CompanySettings):CompanySettings{\n  if(intended===base)return latest;\n  const next={...latest,bank:{...latest.bank}};\n  const keys:Array<Exclude<keyof CompanySettings,'bank'>>=[\n    'nameEn','nameAr','logoDataUrl','addressEn','addressAr','city','country','phone','email','website','vatNumber','taxNumber','commercialRegistration',\n    'signatureDataUrl','stampDataUrl','defaultCurrency','defaultLanguage','defaultPaymentTerms','defaultIncoterm','defaultDeliveryTime','defaultValidityDays','defaultFooterText','defaultNotes'\n  ];\n  for(const key of keys)if(intended[key]!==base[key])(next as any)[key]=intended[key];\n  const bankKeys:Array<keyof CompanySettings['bank']>=['bankName','accountName','iban','swift','currency'];\n  for(const key of bankKeys)if(intended.bank[key]!==base.bank[key])next.bank[key]=intended.bank[key];\n  return next;\n}`;
  const to=`function mergeCompany(base:CompanySettings,intended:CompanySettings,latest:CompanySettings):CompanySettings{\n  if(intended===base)return latest;\n  const next:CompanySettings={...latest,bank:{...latest.bank},bankAccounts:latest.bankAccounts.map(account=>({...account})),commercial:{...latest.commercial,taxPresets:latest.commercial.taxPresets.map(item=>({...item})),paymentTermPresets:latest.commercial.paymentTermPresets.map(item=>({...item})),pricing:{...latest.commercial.pricing}}};\n  const keys:Array<Exclude<keyof CompanySettings,'bank'|'bankAccounts'|'commercial'>>=[\n    'nameEn','nameAr','logoDataUrl','addressEn','addressAr','city','country','phone','email','website','vatNumber','taxNumber','commercialRegistration','defaultBankAccountId',\n    'signatureDataUrl','stampDataUrl','defaultCurrency','defaultLanguage','defaultPaymentTerms','defaultIncoterm','defaultDeliveryTime','defaultValidityDays','defaultFooterText','defaultNotes'\n  ];\n  for(const key of keys)if(intended[key]!==base[key])(next as any)[key]=intended[key];\n  const bankKeys:Array<keyof CompanySettings['bank']>=['bankName','accountName','iban','swift','currency'];\n  for(const key of bankKeys)if(intended.bank[key]!==base.bank[key])next.bank[key]=intended.bank[key];\n  if(JSON.stringify(intended.bankAccounts)!==JSON.stringify(base.bankAccounts))next.bankAccounts=intended.bankAccounts.map(account=>({...account}));\n  if(JSON.stringify(intended.commercial)!==JSON.stringify(base.commercial))next.commercial={...intended.commercial,taxPresets:intended.commercial.taxPresets.map(item=>({...item})),paymentTermPresets:intended.commercial.paymentTermPresets.map(item=>({...item})),pricing:{...intended.commercial.pricing}};\n  return next;\n}`;
  return replaceOnce(source,from,to,'merge company v136');
});

patch('src/lib/documents.ts',source=>{
  source=replaceOnce(source,"import { companySnapshotFrom } from './defaults.js';","import { companySnapshotFrom } from './defaults.js';\nimport { bankDetailsForId, defaultPaymentTermPreset, defaultTaxPreset } from './commercial-controls.js';",'document commercial imports');
  source=replaceOnce(source,"  const validityDays=normalizeValidityDays(company.defaultValidityDays);\n  return {","  const validityDays=normalizeValidityDays(company.defaultValidityDays);\n  const paymentPreset=defaultPaymentTermPreset(company);\n  const taxPreset=defaultTaxPreset(company);\n  return {",'blank document commercial presets');
  source=replaceOnce(source,"    id: makeId('doc'), kind, role:'standard', status: 'draft', lifecycleStatus:'active', revision:1, creditForId:'', creditForNumber:'', voidedAt:'', voidReason:'', number, issueDate,\n    dueDate: kind === 'proforma' ? addDaysIso(issueDate, validityDays) : '',","    id: makeId('doc'), kind, role:'standard', status: 'draft', lifecycleStatus:'active', revision:1, creditForId:'', creditForNumber:'', voidedAt:'', voidReason:'', bankAccountId:company.defaultBankAccountId||'primary', paymentTermPresetId:paymentPreset?.id||'', number, issueDate,\n    dueDate: kind === 'proforma' ? addDaysIso(issueDate, validityDays) : paymentPreset ? addDaysIso(issueDate,paymentPreset.days) : '',",'blank document selector defaults');
  source=replaceOnce(source,"    terms: { incoterm: company.defaultIncoterm, paymentTerms: company.defaultPaymentTerms, packing: '', deliveryTime: company.defaultDeliveryTime, portOfLoading: '', finalDestination: '', countryOfOrigin: '', validity: '', remarks: '' },\n    adjustments: { discountEnabled: false, discountMode: 'fixed', discountValue: '0.00', shippingEnabled: false, shipping: '0.00', otherChargesEnabled: false, otherCharges: '0.00', taxEnabled: false, taxPercent: '0' },","    terms: { incoterm: company.defaultIncoterm, paymentTerms: paymentPreset?.label||company.defaultPaymentTerms, packing: '', deliveryTime: company.defaultDeliveryTime, portOfLoading: '', finalDestination: '', countryOfOrigin: '', validity: '', remarks: '' },\n    adjustments: { discountEnabled: false, discountMode: 'fixed', discountValue: '0.00', shippingEnabled: false, shipping: '0.00', otherChargesEnabled: false, otherCharges: '0.00', taxEnabled: Boolean(taxPreset), taxPercent: taxPreset?.rate||'0' },",'blank tax and term defaults');
  source=replaceOnce(source,"export function refreshCompanySnapshot(doc: LourexDocument, company: CompanySettings): LourexDocument {\n  return { ...doc, companySnapshot: companySnapshotFrom(company), updatedAt: new Date().toISOString() };\n}","export function refreshCompanySnapshot(doc: LourexDocument, company: CompanySettings): LourexDocument {\n  const companySnapshot=companySnapshotFrom(company);\n  const selectedBank=doc.bankAccountId?bankDetailsForId(company,doc.bankAccountId):doc.companySnapshot?.bank;\n  return { ...doc, companySnapshot:{...companySnapshot,bank:selectedBank?{...selectedBank}:companySnapshot.bank}, updatedAt: new Date().toISOString() };\n}",'preserve selected bank on draft refresh');
  return source;
});

patch('src/components/SettingsModal.tsx',source=>{
  source=replaceOnce(source,"import { t } from '../lib/i18n.js';","import { t } from '../lib/i18n.js';\nimport { validateCommercialCompany } from '../lib/commercial-controls.js';\nimport { CommercialControlsSettings } from './CommercialControlsSettings.js';",'settings commercial imports');
  source=replaceOnce(source,"  tab:'company'|'documents'|'security'|'backup';","  tab:'company'|'commercial'|'documents'|'security'|'backup';",'commercial settings tab type');
  source=replaceOnce(source,"  private saveCompany=async()=>{\n    if(!this.state.company.nameEn.trim()&&!this.state.company.nameAr.trim()){this.setState({error:t('Company name is required.','اسم الشركة مطلوب.')});return;}\n    this.setState({busy:true,cleaningAssets:true,error:'',message:'',savedSection:null});","  private saveCompany=async()=>{\n    if(!this.state.company.nameEn.trim()&&!this.state.company.nameAr.trim()){this.setState({error:t('Company name is required.','اسم الشركة مطلوب.')});return;}\n    const commercialError=validateCommercialCompany(this.state.company);if(commercialError){this.setState({error:commercialError});return;}\n    this.setState({busy:true,cleaningAssets:true,error:'',message:'',savedSection:null});",'settings commercial validation');
  source=replaceOnce(source,"{([['company',t('Company','الشركة')],['documents',t('Documents','المستندات')],['security',t('Security','الأمان')],['backup',t('Backup','النسخ الاحتياطي')]] as const)","{([['company',t('Company','الشركة')],['commercial',t('Commercial','تجاري')],['documents',t('Documents','المستندات')],['security',t('Security','الأمان')],['backup',t('Backup','النسخ الاحتياطي')]] as const)",'commercial tab label');
  source=replaceOnce(source,"\n    {this.state.tab==='documents'?<div>","\n    {this.state.tab==='commercial'?<div><div className=\"settings-title\"><div><p className=\"eyebrow\">{t('Commercial','تجاري')}</p><h3>{t('Commercial controls','الضوابط التجارية')}</h3></div>{this.saveButton('company')}</div><CommercialControlsSettings company={c} onChange={company=>this.setState({company,savedSection:null,message:'',error:''})}/></div>:null}\n\n    {this.state.tab==='documents'?<div>",'commercial settings panel');
  return source;
});

patch('src/components/CustomersPage.tsx',source=>{
  source=replaceOnce(source,"import type { Customer, DocumentKind } from '../types.js';","import type { CompanySettings, Customer, DocumentKind } from '../types.js';",'customer company type');
  source=replaceOnce(source,"import { isArabic, t } from '../lib/i18n.js';","import { isArabic, t } from '../lib/i18n.js';\nimport { validateCustomerCommercial } from '../lib/commercial-controls.js';",'customer commercial validator');
  source=replaceOnce(source,"commercialRegistration:'',notes:''}; }","commercialRegistration:'',preferredCurrency:'',paymentTermPresetId:'',paymentTerms:'',paymentDueDays:'',creditLimit:'',creditCurrency:'',notes:''}; }",'blank customer commercial fields');
  source=replaceOnce(source,"interface FormProps { customer: Customer; onChange: (c:Customer)=>void; }\nexport function CustomerForm({customer,onChange}:FormProps): any {","interface FormProps { customer: Customer; company?:CompanySettings; onChange: (c:Customer)=>void; }\nexport function CustomerForm({customer,company,onChange}:FormProps): any {",'customer form company prop');
  const closeBusiness="<Field label={t('Notes','ملاحظات')} className=\"span-2\"><Textarea rows=\"3\" value={customer.notes} onChange={(e:any)=>set('notes',e.target.value)}/></Field></div></section>\n  </div>;";
  const commercialSection="<Field label={t('Notes','ملاحظات')} className=\"span-2\"><Textarea rows=\"3\" value={customer.notes} onChange={(e:any)=>set('notes',e.target.value)}/></Field></div></section>\n    <section className=\"customer-form-section customer-credit-fields\"><div className=\"customer-form-section-heading\"><div><h3>{t('Commercial controls','الضوابط التجارية')}</h3><p>{t('Optional defaults for currency, payment terms and internal credit control.','إعدادات اختيارية للعملة وشروط الدفع والرقابة الداخلية على الائتمان.')}</p></div></div><div className=\"form-grid two customer-form-grid\"><Field label={t('Preferred Currency','العملة المفضلة')}><Input value={customer.preferredCurrency} onChange={(e:any)=>set('preferredCurrency',e.target.value.toUpperCase())}/></Field><Field label={t('Payment Terms','شروط الدفع')}><Input value={customer.paymentTerms} onChange={(e:any)=>set('paymentTerms',e.target.value)}/>{company?.commercial.paymentTermPresets.length?<span className=\"commercial-preset-chips\">{company.commercial.paymentTermPresets.map(preset=><button type=\"button\" key={preset.id} onClick={()=>onChange({...customer,paymentTermPresetId:preset.id,paymentTerms:preset.label,paymentDueDays:String(preset.days),updatedAt:new Date().toISOString()})}>{preset.label}</button>)}</span>:null}</Field><Field label={t('Due in days','الاستحقاق بعد أيام')}><Input type=\"number\" min=\"0\" max=\"3650\" step=\"1\" value={customer.paymentDueDays} onChange={(e:any)=>set('paymentDueDays',e.target.value)}/></Field><Field label={t('Credit Limit','حد الائتمان')}><Input inputMode=\"decimal\" value={customer.creditLimit} placeholder={t('Blank = no limit','فارغ = بدون حد')} onChange={(e:any)=>set('creditLimit',e.target.value)}/></Field><Field label={t('Credit Currency','عملة حد الائتمان')}><Input value={customer.creditCurrency} onChange={(e:any)=>set('creditCurrency',e.target.value.toUpperCase())}/></Field></div></section>\n  </div>;";
  source=replaceOnce(source,closeBusiness,commercialSection,'customer commercial form section');
  source=replaceOnce(source,"interface Props { customers: Customer[]; onSave: (customer:Customer)=>Promise<void>;","interface Props { customers: Customer[]; company:CompanySettings; onSave: (customer:Customer)=>Promise<void>;",'customer page company prop');
  source=replaceOnce(source,"      const haystack=[c.companyNameEn,c.companyNameAr,c.contactPerson,c.email,c.phone,c.city,c.country,c.vatTaxNumber,c.commercialRegistration].join(' ').toLocaleLowerCase();","      const haystack=[c.companyNameEn,c.companyNameAr,c.contactPerson,c.email,c.phone,c.city,c.country,c.vatTaxNumber,c.commercialRegistration,c.preferredCurrency,c.creditCurrency].join(' ').toLocaleLowerCase();",'customer search commercial fields');
  source=replaceOnce(source,"    if(c.email.trim()&&!/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(c.email.trim())){this.setState({error:t('Enter a valid email address or leave it empty.','أدخل بريدًا إلكترونيًا صحيحًا أو اترك الحقل فارغًا.')});return;}\n    this.setState({busy:true,error:''});","    if(c.email.trim()&&!/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(c.email.trim())){this.setState({error:t('Enter a valid email address or leave it empty.','أدخل بريدًا إلكترونيًا صحيحًا أو اترك الحقل فارغًا.')});return;}\n    const commercialError=validateCustomerCommercial(c);if(commercialError){this.setState({error:commercialError});return;}\n    this.setState({busy:true,error:''});",'customer commercial validation');
  source=replaceAllChecked(source,"<CustomerForm customer={this.state.editing}","<CustomerForm company={this.props.company} customer={this.state.editing}",'customer modal company prop');
  return source;
});

patch('src/components/EditorPageCore.tsx',source=>{
  source=replaceOnce(source,"import type { AppSettings, CompanySettings, Customer, DocumentItem, LourexDocument, SavedItem, TemplateId } from '../types.js';","import type { AppSettings, CompanySettings, Customer, DocumentItem, LourexDocument, PaymentRecord, PaymentTermPreset, SavedItem, TemplateId } from '../types.js';",'editor commercial types');
  source=replaceOnce(source,"import { historySuggestions, mergeSavedItemSelections, savedItemSearchText, sortSavedItems } from '../lib/saved-items.js';","import { findSavedItemMatch, historySuggestions, mergeSavedItemSelections, savedItemSearchText, sortSavedItems } from '../lib/saved-items.js';\nimport { addDaysIso } from '../lib/id.js';\nimport { applyCustomerCommercialDefaults, applyPaymentTermPreset, bankAccountIdForDetails, bankAccountsForCompany, bankDetailsForId, customerCreditStatus, paymentTermPresetById, pricingSuggestedUnitPrice } from '../lib/commercial-controls.js';",'editor commercial helpers');
  source=replaceOnce(source,"  document:LourexDocument; documents:LourexDocument[]; customers:Customer[]; company:CompanySettings; savedItems:SavedItem[]; smartDefaults:AppSettings['smartDefaults'];","  document:LourexDocument; documents:LourexDocument[]; customers:Customer[]; company:CompanySettings; savedItems:SavedItem[]; payments:PaymentRecord[]; smartDefaults:AppSettings['smartDefaults'];",'editor payments prop');
  source=replaceOnce(source,"  private field=(key:keyof LourexDocument,value:any)=>this.mutate(d=>({...d,[key]:value}));","  private field=(key:keyof LourexDocument,value:any)=>this.mutate(d=>({...d,[key]:value}));\n  private issueDate=(value:string)=>this.mutate(d=>{let next={...d,issueDate:value};if(d.kind!=='invoice')return next;const preset=paymentTermPresetById(this.props.company,d.paymentTermPresetId);if(preset)return applyPaymentTermPreset(next,preset);const customer=this.props.customers.find(item=>item.id===d.customerSnapshot?.sourceCustomerId);if(customer&&/^\\d+$/.test(customer.paymentDueDays.trim()))next={...next,dueDate:addDaysIso(value,Math.min(3650,Number(customer.paymentDueDays)))};return next;});\n  private applyTermPreset=(preset:PaymentTermPreset)=>this.mutate(d=>applyPaymentTermPreset(d,preset));\n  private selectBank=(id:string)=>{const bank=bankDetailsForId(this.props.company,id);if(!bank)return;this.mutate(d=>({...d,bankAccountId:id,companySnapshot:{...d.companySnapshot,bank:{...bank}}}));};",'editor commercial methods');
  source=replaceOnce(source,"  private selectCustomer=(c:Customer)=>{this.mutate(d=>({...d,customerSnapshot:customerSnapshotFrom(c)}));this.setState({customerQuery:(isArabic()?c.companyNameAr:c.companyNameEn)||c.companyNameEn||c.companyNameAr,customerOpen:false});};","  private selectCustomer=(c:Customer)=>{this.mutate(d=>applyCustomerCommercialDefaults({...d,customerSnapshot:customerSnapshotFrom(c)},c,this.props.company));this.setState({customerQuery:(isArabic()?c.companyNameAr:c.companyNameEn)||c.companyNameEn||c.companyNameAr,customerOpen:false});};",'editor customer defaults');
  source=replaceOnce(source,"    const hsCodeSuggestions=priorHsCodes(this.props.savedItems,this.props.documents);","    const hsCodeSuggestions=priorHsCodes(this.props.savedItems,this.props.documents);\n    const bankOptions=bankAccountsForCompany(this.props.company);\n    const selectedBankId=d.bankAccountId||bankAccountIdForDetails(this.props.company,d.companySnapshot.bank)||this.props.company.defaultBankAccountId||'primary';\n    const credit=customerCreditStatus(d,this.props.customers,this.props.documents,this.props.payments);",'editor commercial render state');
  source=replaceOnce(source,"<Input type=\"date\" value={d.issueDate} onChange={(e:any)=>this.field('issueDate',e.target.value)}/>","<Input type=\"date\" value={d.issueDate} onChange={(e:any)=>this.issueDate(e.target.value)}/>",'issue date automation');
  const customerClose="</section>\n\n        <section className={`editor-section ${sectionHasError('items','item-')?'section-has-error':''}`}>";
  const customerCredit="</section>{credit?<div className={`credit-limit-banner ${credit.exceeded?'is-over':''} ${!credit.comparable?'is-unmatched':''}`}><span className=\"credit-limit-icon\"><Icon name=\"invoice\" size={16}/></span><div><strong>{t('Customer credit control','رقابة ائتمان العميل')}</strong>{credit.comparable?<span>{t(`Outstanding ${credit.outstanding} + this invoice ${credit.candidate} = ${credit.projected} ${credit.currency}`,`المستحق ${credit.outstanding} + هذه الفاتورة ${credit.candidate} = ${credit.projected} ${credit.currency}`)}</span>:<span>{t(`Limit is ${credit.limit} ${credit.creditCurrency}; this invoice is ${credit.currency}. No FX comparison is made.`,`الحد ${credit.limit} ${credit.creditCurrency} وهذه الفاتورة ${credit.currency}. لن تتم مقارنة العملات بدون سعر صرف.`)}</span>}</div><b>{credit.comparable?t(`Limit ${credit.limit}`,`الحد ${credit.limit}`):t('Different currency','عملة مختلفة')}</b></div>:null}\n\n        <section className={`editor-section ${sectionHasError('items','item-')?'section-has-error':''}`}>";
  source=replaceOnce(source,customerClose,customerCredit,'editor credit banner');
  source=replaceOnce(source,"const zeroPrice=i.unitPrice.trim()!==''&&isDecimalInput(i.unitPrice)&&decimalToScaled(i.unitPrice)===0n;const suggestions=","const zeroPrice=i.unitPrice.trim()!==''&&isDecimalInput(i.unitPrice)&&decimalToScaled(i.unitPrice)===0n;const savedMatch=findSavedItemMatch(this.props.savedItems,i);const pricingCost=(i.unitCost||'').trim()||(savedMatch?.lastCostCurrency===d.currency?(savedMatch.lastUnitCost||''):'');const suggestedPrice=pricingSuggestedUnitPrice(pricingCost,this.props.company.commercial.pricing);const suggestions=",'editor pricing suggestion calculation');
  const priceField="<Field label={t(`Unit Price (${d.currency})`,`سعر الوحدة (${d.currency})`)} className=\"required-field\" error={error(`item-${index}-price`)}><Input inputMode=\"decimal\" enterKeyHint=\"done\" value={i.unitPrice} onChange={(e:any)=>this.item(i.id,'unitPrice',e.target.value)}/></Field>";
  const priceControl="<div className=\"item-price-control\"><Field label={t(`Unit Price (${d.currency})`,`سعر الوحدة (${d.currency})`)} className=\"required-field\" error={error(`item-${index}-price`)}><Input inputMode=\"decimal\" enterKeyHint=\"done\" value={i.unitPrice} onChange={(e:any)=>this.item(i.id,'unitPrice',e.target.value)}/></Field>{suggestedPrice&&suggestedPrice!==i.unitPrice?<button type=\"button\" className=\"pricing-suggestion-chip\" onClick={()=>this.item(i.id,'unitPrice',suggestedPrice)}>{t(`Use suggested ${suggestedPrice}`,`استخدم المقترح ${suggestedPrice}`)}</button>:null}</div>";
  source=replaceOnce(source,priceField,priceControl,'editor pricing suggestion UI');
  const taxInput="{d.adjustments.taxEnabled?<Input inputMode=\"decimal\" value={d.adjustments.taxPercent} onChange={(e:any)=>this.adj('taxPercent',e.target.value)}/>:null}";
  const taxControls="{d.adjustments.taxEnabled?<div><Input inputMode=\"decimal\" value={d.adjustments.taxPercent} onChange={(e:any)=>this.adj('taxPercent',e.target.value)}/>{this.props.company.commercial.taxPresets.length?<span className=\"commercial-preset-chips\">{this.props.company.commercial.taxPresets.map(preset=><button type=\"button\" key={preset.id} onClick={()=>this.mutate(doc=>({...doc,adjustments:{...doc.adjustments,taxEnabled:true,taxPercent:preset.rate}}))}>{preset.name} {preset.rate}%</button>)}</span>:null}</div>:null}";
  source=replaceOnce(source,taxInput,taxControls,'tax preset controls');
  const paymentField="<Field label={t('Payment Terms','شروط الدفع')}><Input value={d.terms.paymentTerms} onChange={(e:any)=>this.term('paymentTerms',e.target.value)}/></Field>";
  const paymentAndBank="<Field label={t('Payment Terms','شروط الدفع')}><Input value={d.terms.paymentTerms} onChange={(e:any)=>this.mutate(doc=>({...doc,paymentTermPresetId:'',terms:{...doc.terms,paymentTerms:e.target.value}}))}/>{this.props.company.commercial.paymentTermPresets.length?<span className=\"commercial-preset-chips\">{this.props.company.commercial.paymentTermPresets.map(preset=><button type=\"button\" key={preset.id} onClick={()=>this.applyTermPreset(preset)}>{preset.label}</button>)}</span>:null}</Field><Field label={t('Bank Account','الحساب البنكي')}><Select value={selectedBankId} onChange={(e:any)=>this.selectBank(e.target.value)}>{bankOptions.map(account=><option key={account.id} value={account.id}>{account.label} · {account.currency}</option>)}</Select><small className=\"bank-account-select-note\">{t('This selection is snapshotted into the document and does not change issued invoices later.','يتم حفظ هذا الاختيار داخل المستند ولا يتغير في الفواتير الصادرة لاحقًا.')}</small></Field>";
  source=replaceOnce(source,paymentField,paymentAndBank,'payment presets and bank choice');
  source=replaceAllChecked(source,"<CustomerForm customer={this.state.addCustomer}","<CustomerForm company={this.props.company} customer={this.state.addCustomer}",'editor quick customer company prop');
  return source;
});

patch('src/app/App.tsx',source=>{
  source=replaceOnce(source,"import { assertInvoicePaymentInvariant, normalizePaymentRecord } from '../lib/payments.js';","import { assertInvoicePaymentInvariant, normalizePaymentRecord } from '../lib/payments.js';\nimport { applyCustomerCommercialDefaults, applyPaymentTermPreset, assertCustomerCreditLimit, paymentTermPresetByLabel } from '../lib/commercial-controls.js';",'app commercial imports');
  const reserve="private reserveDocument=async(kind:DocumentKind):Promise<{doc:LourexDocument;vault:VaultPayload}>=>{const vault=this.requireVault();const next=nextDocumentNumber(vault,kind);await this.persist(next.vault);const current=this.requireVault();const smart=current.appSettings.smartDefaults;const base=createBlankDocument(kind,next.number,current.company);const doc={...base,currency:smart.currency||base.currency,language:smart.language||base.language,terms:{...base.terms,incoterm:smart.incoterm,paymentTerms:smart.paymentTerms,deliveryTime:smart.deliveryTime},appearance:{...base.appearance,templateId:kind==='proforma'?smart.quoteTemplateId:smart.invoiceTemplateId}};return{doc,vault:current};};";
  const reserveNew="private reserveDocument=async(kind:DocumentKind):Promise<{doc:LourexDocument;vault:VaultPayload}>=>{const vault=this.requireVault();const next=nextDocumentNumber(vault,kind);await this.persist(next.vault);const current=this.requireVault();const smart=current.appSettings.smartDefaults;const base=createBlankDocument(kind,next.number,current.company);const paymentTerms=smart.paymentTerms||base.terms.paymentTerms;let doc={...base,currency:smart.currency||base.currency,language:smart.language||base.language,terms:{...base.terms,incoterm:smart.incoterm,paymentTerms,deliveryTime:smart.deliveryTime},appearance:{...base.appearance,templateId:kind==='proforma'?smart.quoteTemplateId:smart.invoiceTemplateId}};const paymentPreset=paymentTermPresetByLabel(current.company,paymentTerms);if(paymentPreset)doc=applyPaymentTermPreset(doc,paymentPreset);return{doc,vault:current};};";
  source=replaceOnce(source,reserve,reserveNew,'app reserve commercial defaults');
  source=replaceOnce(source,"private newDocumentForCustomer=async(kind:DocumentKind,customer:Customer)=>{try{const {doc}=await this.reserveDocument(kind);const prepared={...doc,customerSnapshot:customerSnapshotFrom(customer)};","private newDocumentForCustomer=async(kind:DocumentKind,customer:Customer)=>{try{const {doc}=await this.reserveDocument(kind);const prepared=applyCustomerCommercialDefaults({...doc,customerSnapshot:customerSnapshotFrom(customer)},customer,this.requireVault().company);",'customer-specific document defaults');
  source=replaceOnce(source,"const updated={...doc,role:doc.role||'standard',lifecycleStatus:doc.lifecycleStatus||'active',revision:Math.max(1,doc.revision||1),creditForId:doc.creditForId||'',creditForNumber:doc.creditForNumber||'',voidedAt:doc.voidedAt||'',voidReason:doc.voidReason||'',updatedAt:new Date().toISOString()};assertInvoicePaymentInvariant(updated,vault.payments,vault.documents);","const updated={...doc,role:doc.role||'standard',lifecycleStatus:doc.lifecycleStatus||'active',revision:Math.max(1,doc.revision||1),creditForId:doc.creditForId||'',creditForNumber:doc.creditForNumber||'',voidedAt:doc.voidedAt||'',voidReason:doc.voidReason||'',bankAccountId:doc.bankAccountId||'',paymentTermPresetId:doc.paymentTermPresetId||'',updatedAt:new Date().toISOString()};if(updated.status==='final'&&existing?.status!=='final')assertCustomerCreditLimit(updated,vault.customers,vault.documents,vault.payments);assertInvoicePaymentInvariant(updated,vault.payments,vault.documents);",'credit limit issuance guard');
  source=replaceOnce(source,"<CustomersPage customers={vault.customers}","<CustomersPage customers={vault.customers} company={vault.company}",'customers company prop');
  return source;
});

patch('index.html',source=>replaceOnce(source,'  <link rel="stylesheet" href="./styles/reports-v135.css" />\n  <link rel="stylesheet" href="./styles/performance-polish-v100.css" />','  <link rel="stylesheet" href="./styles/reports-v135.css" />\n  <link rel="stylesheet" href="./styles/commercial-controls-v136.css" />\n  <link rel="stylesheet" href="./styles/performance-polish-v100.css" />','v136 stylesheet order'));

patch('public/sw.js',source=>{
  source=replaceOnce(source,'// v135 — multi-currency financial reporting; retains v134 profitability, v133 receivables, v132 document lifecycle and v131 payments.','// v136 — commercial controls for pricing, tax, banks, payment terms and customer credit; retains v135 reporting.','v136 service worker comment');
  source=replaceOnce(source,"// Legacy regression markers only; active runtime cache is v135: const CACHE = 'lourex-invoice-v101'; const CACHE = 'lourex-invoice-v120'; const CACHE = 'lourex-invoice-v131'; const CACHE = 'lourex-invoice-v132'; const CACHE = 'lourex-invoice-v133'; const CACHE = 'lourex-invoice-v134';","// Legacy regression markers only; active runtime cache is v136: const CACHE = 'lourex-invoice-v101'; const CACHE = 'lourex-invoice-v120'; const CACHE = 'lourex-invoice-v131'; const CACHE = 'lourex-invoice-v132'; const CACHE = 'lourex-invoice-v133'; const CACHE = 'lourex-invoice-v134'; const CACHE = 'lourex-invoice-v135';",'v136 cache compatibility markers');
  source=replaceOnce(source,"const CACHE = 'lourex-invoice-v135';","const CACHE = 'lourex-invoice-v136';",'active v136 cache');
  source=replaceOnce(source,'"./styles/reports-v135.css","./styles/performance-polish-v100.css"','"./styles/reports-v135.css","./styles/commercial-controls-v136.css","./styles/performance-polish-v100.css"','v136 css offline');
  source=replaceOnce(source,'"./src/components/ReportsPage.js","./src/components/SavedItemsPage.js"','"./src/components/ReportsPage.js","./src/components/CommercialControlsSettings.js","./src/components/SavedItemsPage.js"','commercial settings offline');
  source=replaceOnce(source,'"./src/lib/profitability.js","./src/lib/reports.js","./src/lib/document-lifecycle.js"','"./src/lib/profitability.js","./src/lib/reports.js","./src/lib/commercial-controls.js","./src/lib/document-lifecycle.js"','commercial logic offline');
  return source;
});

patch('tests/reports-v135.test.mjs',source=>replaceOnce(source,"assert.ok(/^const CACHE = 'lourex-invoice-v135';/m.test(sw));assert.ok(sw.includes(\"const CACHE = 'lourex-invoice-v134'\"));","assert.ok(/^const CACHE = 'lourex-invoice-v\\d+';/m.test(sw));assert.ok(sw.includes(\"const CACHE = 'lourex-invoice-v135'\"));assert.ok(sw.includes(\"const CACHE = 'lourex-invoice-v134'\"));",'reports cache forward compatibility'));
