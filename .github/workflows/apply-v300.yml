name: Apply LOUREX v300 branch patch

on:
  push:
    branches:
      - feature/home-clean-rebuild-v284
    paths:
      - scripts/apply-v300.mjs
      - scripts/apply-v300-continue.mjs
      - .github/workflows/apply-v300.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - run: npm ci
      - name: Apply v300 source changes
        run: |
          node scripts/apply-v300.mjs || true
          node scripts/apply-v300-continue.mjs
      - name: Typecheck
        run: |
          set +e
          npm run typecheck > v300-typecheck.txt 2>&1
          status=$?
          cat v300-typecheck.txt
          if [ "$status" -ne 0 ]; then
            git config user.name "LOUREX v300 automation"
            git config user.email "actions@users.noreply.github.com"
            git add -f v300-typecheck.txt
            git commit -m "chore: capture v300 typecheck failure"
            git push origin HEAD:feature/home-clean-rebuild-v284
            exit "$status"
          fi
          rm -f v300-typecheck.txt
      - name: Diff safety check
        run: git diff --check
      - name: Commit v300 implementation
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "LOUREX v300 automation"
          git config user.email "actions@users.noreply.github.com"
          git add index.html src tests
          git commit -m "feat: add purchase orders attachments and login PIN gate v300"
          git push origin HEAD:feature/home-clean-rebuild-v284
