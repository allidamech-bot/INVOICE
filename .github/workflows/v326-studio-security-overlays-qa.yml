name: LOUREX v326 Studio Security Overlays QA

on:
  push:
    branches:
      - feature/v326-foundation-recovery

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
      - run: npm ci
      - name: Security gate
        run: node scripts/security-check.mjs
      - name: TypeScript
        run: npm run typecheck
      - name: Production build
        run: npm run build
      - name: v326 design contracts
        run: |
          node --test tests/v326-foundation-recovery.test.mjs
          node --test tests/v326-business-workspaces.test.mjs
      - name: Install Chromium and WebKit
        run: |
          npm install --no-save --package-lock=false playwright@1.55.0
          npx playwright install --with-deps chromium webkit
      - name: Studio security overlays visual QA
        run: |
          python3 -m http.server 4173 > /tmp/lourex-v326-studio-server.log 2>&1 &
          server_pid=$!
          trap 'kill "$server_pid" >/dev/null 2>&1 || true' EXIT
          ready=0
          for attempt in $(seq 1 30); do
            if curl --fail --silent http://127.0.0.1:4173/tests/visual/obsidian-editor.html >/dev/null; then ready=1; break; fi
            sleep 1
          done
          if [ "$ready" -ne 1 ]; then cat /tmp/lourex-v326-studio-server.log; exit 1; fi
          node tests/visual/run-v326-studio-security-overlays.cjs
      - name: Upload v326 studio evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: lourex-v326-studio-security-overlays-qa
          path: visual-qa-output/v326-studio-security-overlays
          if-no-files-found: ignore
